name: Deploy build/ via FTP

on:
  push:
    branches:
      - push-server

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload build folder via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp://vps114489.serveur-vps.net/::sise-pdzsta.net/
          username: pdzstaadmin
          password: zrsE49H@
          local-dir: ./PDZSTA_map/
          server-dir: /PDZSTA_map/
          protocol: ftp
